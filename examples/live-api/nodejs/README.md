# Node.js live client

Node.js version of the `index.html` live client flow (WebSocket setup, mic streaming, text turns, audio playback) without noise gate.

## Run

```bash
npm install
npm start
```

Mic langsung ON saat start. Command CLI: `/quit`, atau ketik pesan biasa.
