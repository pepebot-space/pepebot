# Python live client

Python version of the `index.html` live client flow (WebSocket setup, mic streaming, text turns, audio playback) without noise gate.

## Run

```bash
poetry install
poetry run python main.py
```

Mic langsung ON saat start. Command CLI: `/quit`, atau ketik pesan biasa.
